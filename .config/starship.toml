format = """
[](bg:blue fg:white)\
$username\
$directory\
$git_branch\
$git_status\
$time\
[🭡](bg:none fg:bright-black)
> 
"""

# Disable the blank line at the start of the prompt
# add_newline = false

# You can also replace your username with a neat symbol like   or disable this
# and use the os module below
palette = 'ieremii'

[palettes.ieremii]
color_fg0 = '#fbf1c7'
color_bg1 = '#3c3836'
color_bg3 = '#665c54'
color_blue = '#458588'
color_aqua = '#689d6a'
color_green = '#98971a'
color_orange = '#d65d0e'
color_purple = '#b16286'
color_red = '#cc241d'
color_yellow = '#d79921'

[username]
show_always = true
style_user = "bg:blue fg:bright-gray"
style_root = "bg:#9A348E"
format = '[ $user 🭅]($style)'
disabled = false

[directory]
style = "bg:bright-gray fg:bright-blue"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"

[git_branch]
symbol = ""
style = "bg:bright-blue fg:gray"
format = '[🭐  $symbol $branch  🭅]($style)'

[git_status]
style = "bg:gray fg:blue"
format = '[ $all_status$ahead_behind ]($style)'

[time]
disabled = false
time_format = "%R" # Hour:Minute Format
style = "bg:blue fg:white"
format = '[🭐 $time ]($style)'
